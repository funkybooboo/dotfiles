# 000060-usbguard-opensnitch.sh — USBGuard + OpenSnitch (DISABLED)
# Installs: (disabled — deferred to a future migration)
# Links:    (disabled — deferred to a future migration)
# Enables:  (disabled)
# Note: USBGuard + OpenSnitch are intentionally not installed or linked here.
#       They will be added as a new migration at a later date.

[[ -n "${_COMMON_LOADED:-}" ]] || source "$(dirname "${BASH_SOURCE[0]}")/_common.sh"

section "USBGuard + OpenSnitch"

info "usbguard + opensnitch deferred (skipping install + link + enable)"
skip "usbguard + opensnitch (deferred to a future migration)"
