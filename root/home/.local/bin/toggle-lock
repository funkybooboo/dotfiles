#!/bin/bash
if systemctl --user is-active --quiet hypridle; then
    systemctl --user stop hypridle
    echo "Lockscreen disabled"
else
    systemctl --user start hypridle
    echo "Lockscreen enabled"
fi
